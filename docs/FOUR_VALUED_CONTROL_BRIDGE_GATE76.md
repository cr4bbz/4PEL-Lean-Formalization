# Gate 76: Four-Valued Epistemic-Control Bridge

Gate 76 translates the Bayesian alias witness back into 4PEL status.

For the proposition "the hidden state is robust":

```text
positive support = posterior mass on robust
negative support = posterior mass on fragile
threshold        = 3/4
```

The symmetric prior has neither side above threshold, hence `N`. The Gate-69 optimal stress probe then produces:

```text
Recovery     -> T
Non-Recovery -> F
```

Thus the verified controller induces the transition pattern:

```text
N -> T/F
```

## Interpretation

Optimal sensing is now visible inside the four-valued semantics itself. The controller buys information because it can move a proposition out of an epistemic gap, not merely because a posterior number increases.

## Boundary

The mapping is proposition-specific and uses a declared support interpretation. Gate 77 treats the distinct problem of resolving a genuine `B` conflict with independent positive and negative evidence channels.
